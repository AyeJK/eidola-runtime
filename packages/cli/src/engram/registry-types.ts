export interface EngramListEntry {
  id: string;
  name?: string;
  description?: string;
  author?: string;
  previewPath?: string;
  vesselType?: string;
  engramDir: string;
  vesselsDir: string;
}
